# ===== TWILIO PRO SYSTEM CONFIGURATION =====
# Thay đổi những giá trị này theo môi trường deploy của bạn

# 1. DATABASE - Connection string cho PostgreSQL
# Replit: Tự động có sẵn
# VPS: postgresql://user:password@localhost:5432/database_name  
# Hosting: External database URL (Neon, Supabase, etc)
DATABASE_URL=postgresql://username:password@host:port/database_name

# 2. DOMAIN - Website domain cho Twilio webhooks (QUAN TRỌNG!)
# Replit: Để trống (tự động detect)
# VPS: DOMAIN=yourdomain.com
# Hosting: DOMAIN=yourdomain.com
DOMAIN=

# 3. JWT SECRET - Bảo mật authentication (REQUIRED!)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Example: a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456
JWT_SECRET=generate-your-own-64-char-random-string-here

# 4. APPLICATION SETTINGS
NODE_ENV=production
PORT=5000

# ===== DEVELOPMENT ONLY =====
# Uncomment cho development
# NODE_ENV=development
# DOMAIN=localhost:5000